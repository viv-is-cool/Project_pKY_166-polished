version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - BACKEND_PORT=8000
      - OLLAMA_HOST=${OLLAMA_HOST}
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    environment:
      - VITE_API_PORT=8000
